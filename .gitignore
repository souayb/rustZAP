# Build artifacts
/target
/dist

# Local editor / assistant
.claude/settings.local.json
# ruflo / claude-flow generated runtime state (per-machine tuning + telemetry)
.claude-flow/
.claude/proven-config.json
.claude/.proven-config-version
.idea/
.vscode/
*.swp
*~
.DS_Store
Thumbs.db

# Secrets / local env (never commit)
.env
.env.*
!.env.example

# Generated scanner / analyzer / stress output (CLI, TUI, docker compose)
/report.json
/report.csv
/report.html
/*-report.json
/*-report.csv
/*-report.html
/*.sarif
.rustzap-gitleaks-report.json
**/.rustzap-gitleaks-report.json
/reports/*
!/reports/.gitkeep

# Agent scope files are environment-specific and list real targets — the
# documented template `scope.example.yaml` is committed; a real `scope.yaml` is not.
/scope.yaml
/scope.yml
agent-report*.json
agent-report*.captures.json
agent-trace*.jsonl

# Local clones used for analysis (not part of the product)
public_repos/

# Draft / local notes
FEATURES.md
CHANGELOG.md
node_modules/
vscode-extension/out/
