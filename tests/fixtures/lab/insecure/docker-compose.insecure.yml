# Deliberately-insecure compose — SAST bait (iac/native). Never deployed.
services:
  app:
    build: .
    privileged: true
    network_mode: host
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
