# Committed sample env — copied to .env at build/run time (real .env stays
# uncommitted). Named `dotenv.sample` so it doesn't trip the pre-commit
# forbidden-file guard, while still giving secret scanners a target. Fake creds.
DATABASE_URL=postgres://admin:s3cr3tpassword@db.internal:5432/vitalpulse
JWT_SECRET=super-secret-signing-key-do-not-use
STRIPE_KEY=set-in-env-never-hardcode
